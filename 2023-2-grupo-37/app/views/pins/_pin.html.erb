<div id="<%= dom_id pin %>">
  <p>
    <strong>Title:</strong>
    <%= pin.title %>
  </p>

  <%= link_to image_tag(pin.image_as_thumbnail), pin.image %> 
  <%# <%= link_to image_tag(pin.image) %> 

</div>
