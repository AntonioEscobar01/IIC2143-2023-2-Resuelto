<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Proyecto Grupo 37</title>
  </head>
  <body>
    <%= render 'shared/header' %>
    <main>
        <div class="div-botones-admin">
                <a href="/admin/productos">
                    <button>
                        Mostrar Productos
                        <div class="arrow-wrapper">
                            <div class="arrow"></div>
                        </div>
                    </button>
                </a>

                <a href="/admin/usuarios">
                    <button>
                        Mostrar Usuarios
                        <div class="arrow-wrapper">
                            <div class="arrow"></div>
                        </div>
                    </button>
                </a>

                <a href="/admin/intercambios">
                    <button class="selected">
                        Mostrar Intercambios
                        <div class="arrow-wrapper">
                            <div class="arrow"></div>
                        </div>
                    </button>
                </a>

                <a href="/admin/reportes">
                    <button>
                        Mostrar Reportes
                        <div class="arrow-wrapper">
                            <div class="arrow"></div>
                        </div>
                    </button>
                </a>
        </div>
        
        <section class="element-admin">
            <% if Intercambio.count > 0 %>
                <% @intercambios.each do |intercambio| %>
                    <div class="elements-admin">
                        <h2> Producto: <%= Producto.find(intercambio.producto_id).nombre %></h2>
                        <p> Estado: <%= intercambio.estado %></p>
                        <div class="div-botones-admin-2">
                            <%= button_to 'Editar', editar_intercambio_path(intercambio.id), method: :get %>
                            <%= button_to 'Eliminar', eliminar_intercambio_path(intercambio.id), method: :get %>
                        </div>
                    </div>
                <% end %>
            <% else %>
                <div class="btn">NO HAY INTERCAMBIOS</div>
            <% end %>
        </section>
    </main>